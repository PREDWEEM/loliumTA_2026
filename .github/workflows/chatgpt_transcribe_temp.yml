name: ChatGPT temporary transcription test
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test runner
        run: echo "CHATGPT_TRANSCRIPTION_RUNNER_OK"
